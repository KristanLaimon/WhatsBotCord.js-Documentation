---
  title: "Commands"
  description: "How can you create more customized commands?" 
---

This far you should have your shiny new command `!ping` from the [last chapter 'Quickstart'](/getting-started/quickstart),
but of course you'd like to do more than just "!ping", let's dive into examples about what else you can do with your commands.

# Sending

We've sent text messages, but we can send all kinds of messages (all from whatsapp of course) like:
